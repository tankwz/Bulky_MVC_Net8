@model List<Category>
<a href="c:\users\tantk\source\repos\bulky\pj.models\product.cs">c:\users\tantk\source\repos\bulky\pj.models\product.cs</a>
<div class="container">
    <div class="row pt-3">
        <div class="col-6">
            <h2 class="text-primary">
                Category
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Category" asp-action="Create" class="btn btn-primary"> <i class="bi bi-plus-circle"></i>&nbsp; Create New Category</a>
        </div>
    </div>

    <table class="table table-bordered table-striped table-dark">
        <thead>
            <tr>
                <th class="">
                    Category Name
                </th>
                <th class="">
                    Display Order
                </th>
                <th class="">
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model.OrderBy(u => u.DisplayOrder))
            {
                <tr>
                    <td class="">
                        @obj.Name
                    </td>
                    <td class="">@obj.DisplayOrder</td>
                    <td>
                        <div class="w-100 btn-group" role="group">
                            <a asp-controller="Category" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2"><i class="bi bi-pencil"></i> &nbsp Edit</a>
                            <a asp-controller="Category" asp-action="oldDelete" asp-route-id="@obj.Id" class="btn btn-danger mx-2"><i class="bi bi-trash3"></i> &nbsp Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>